Codebase for "Channel-selection using GumbelSoftmax" (previously titled "An end-to-end approach for speeding up neural networks").

Code uses PyTorch 0.3.1. We timed it and found that this code is faster in 0.3.1 than 0.4.1. Have not tested it with 1+ yet.
